
  # Restaurant Management System Prototype

  This is a code bundle for Restaurant Management System Prototype. The original project is available at https://www.figma.com/design/kHKDWbXoUMGC89OYy40LIu/Restaurant-Management-System-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  